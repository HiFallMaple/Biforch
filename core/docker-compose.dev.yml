# docker-compose.dev.yml
services:
  core:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - ./core:/app/core
      - ./data:/app/data
    env_file:
      - .env
    ports:
      - "$PORT:8000"
