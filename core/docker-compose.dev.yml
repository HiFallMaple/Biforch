# docker-compose.dev.yml
services:
  core:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - ./core:/app/core
      - ./data:/app/data
    env_file:
      - .env
    ports:
      - "$PORT:8000"
    cpus: "0.50"          # 最多使用半顆 CPU
    mem_limit: 256m    