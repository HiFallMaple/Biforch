# docker-compose.prod.yml

services:
  reverse-proxy-agent:
    build:
      context: .
      dockerfile: Dockerfile.prod
    env_file:
      - .env
    ports:
      - "$PORT:8000"
    volumes:
      - ./data:/app/data
    restart: always